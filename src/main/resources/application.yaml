spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/testing_with_spring_boot
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect